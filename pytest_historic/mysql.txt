
show databases;
use tr69_functional;

drop table IF EXISTS params_ref;
CREATE TABLE IF NOT EXISTS params_ref (
	id int NOT NULL AUTO_INCREMENT,
	model VARCHAR(256) NOT NULL,
	static VARCHAR(50),
	val_ref  VARCHAR(256),
	PRIMARY KEY (id, model)
);

use tr69_functional;
drop table IF EXISTS params_act;
CREATE TABLE IF NOT EXISTS params_act (
	id int NOT NULL AUTO_INCREMENT,
	model VARCHAR(256) NOT NULL,
	static VARCHAR(50),
	val_act  VARCHAR(200) NOT NULL,
	project  VARCHAR(200) NOT NULL,
	android  VARCHAR(200) NOT NULL,
	version  VARCHAR(200) NOT NULL,
	date  VARCHAR(200) NOT NULL,
	PRIMARY KEY (id, model, project, android)
);
show databases;
use tr69_functional;
show tables;
# select * from params_ref;
select * from params_act;
